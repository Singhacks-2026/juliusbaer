# Deployment manifest (Streamlit Community Cloud auto-installs from this file).
# The core signal engine (wealth_intelligence/) needs NONE of these — it is pure
# standard library. These are only for the Streamlit workbench and the
# Claude-grounded explanations. Mirrors requirements-app.txt.
streamlit>=1.36
anthropic>=0.69
